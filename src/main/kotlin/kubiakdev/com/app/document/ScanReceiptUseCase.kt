package kubiakdev.com.app.document

class ScanReceiptUseCase {

    suspend fun scanReceipt(fileBytes: ByteArray) {
        /* todo optimize image
            send image to AI to get JSON
            extract JSON
         */
    }
}